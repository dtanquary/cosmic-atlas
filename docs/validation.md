# Prototype validation — 10 September 2026

The first prototype uses the full accepted DESI DR1 galaxy catalog. Local numerical, dataset-integrity, production-build, and browser checks passed. Remaining checks are identified below rather than inferred from the results.

## Data correctness

- Official FITS source: 22,371,272,640 bytes; 28,425,963 records. SHA-256: `2d95ad99361039b556c402b49e0e7c84df5f00106dc5731d44476a58b128b49b`.
- Accepted: **14,140,375 unique primary galaxy targets**. Sequential exclusions and exact filters are recorded in the manifest.
- Full catalog: 1,010 hierarchy nodes, 984,457,330 compressed asset bytes. The deterministic development dataset contains 1,000,000 galaxies.
- Direct Astropy comparison: maximum measured distance-interpolation error **1.772 × 10⁻⁷ Mpc**, below the 10⁻⁴ Mpc numerical budget. This is computational error, not observational uncertainty.
- Maximum render-coordinate rounding error: **0.0002441 Mpc per axis** in the coarsest nodes. Inspection and measurement retain float64 source-derived positions.
- Full and development dataset validators passed: every compressed checksum and header, row count, coordinate reconstruction, node bounds, ID uniqueness, child accounting, and exactly-once leaf coverage.
- Seven TypeScript tests and four Python numerical tests passed. Strict TypeScript and the production build passed. The prebuild check verifies active asset presence and declared byte counts.

## Browser checks

Actual browsers on this Mac were used, including GPU ID rendering/readback. Each passed selecting two distinct points, exact 64-bit target ID display, measurement persistence across detail changes, WebGL context loss/recovery, and rejection of corrupted data.

| Browser | Dataset tested | Result |
| --- | --- | --- |
| Chrome 152 | Full 14,140,375 catalog | Pass |
| Safari 27 | 1,000,000 development sample | Pass |
| Firefox 149 | 1,000,000 development sample | Pass |

The tests exposed and fixed a Retina picking error: the GPU pick scissor rectangle was being scaled twice. Picking now uses physical render-target coordinates once. Context recovery restores the map and dismisses the recovery overlay. A measurement of 915.7522950655592 Mpc remained identical through a detail-mode transition in Chrome.

## Measured performance

Apple M3 Max, 36 GB system memory, Chrome/ANGLE Metal, 1920 × 1080 drawing buffer. Continuous overview orbit after five seconds of warmup, sampled for twenty seconds. These are local steady-state results, not minimum-device guarantees.

| Mode | Submitted points | Mean FPS | p95 frame time | Draw calls | Managed allocations |
| --- | ---: | ---: | ---: | ---: | ---: |
| Adaptive | 1,990,709 | 120.0 | 10.3 ms | 142 | 436.9 MiB |
| Full detail | 14,140,375 | 20.8 | 59.5 ms | 883 | 436.4 MiB |

Both runs had no pending/failed chunks or memory-limit state at measurement end. Full detail reported complete coverage. Adaptive remained explicitly sampled. Managed allocations describe the application's tracked arrays, GPU attributes, metadata, and reservations; they are not total browser-process memory or all driver overhead.

The earliest observed coarse view in the full-catalog Chrome run appeared at 168 ms on localhost. That result does **not** verify the three-second target at a controlled 100 Mbps. The browser checks also observed 429 ms in Safari and 366 ms in Firefox using the smaller development dataset.

## Remaining checks and scope limits

- Manual pointer-lock flight, dense-region/fly performance, controlled-network startup, and a long cache-eviction soak still need dedicated acceptance runs. The native desktop window connection became unavailable during the final manual pass.
- Safari and Firefox compatibility passed on the development dataset; full-catalog stress benchmarks were run only in Chrome.
- Optional WebMCP registration was unavailable in all three browser environments, so those optional tools have not been exercised there. Normal controls do not depend on WebMCP.
- Mobile navigation, broad resolved-galaxy coverage, and completeness beyond the selected DESI release are outside this prototype's scope. The subsequent single-galaxy close-up is recorded below.

Raw browser evidence is retained in [validation-results.json](validation-results.json). Reproduction commands and development-only diagnostic routes are in the [README](../README.md).

## Delivery status

The full prototype is running locally at `http://127.0.0.1:5173/`. The production archive passed manifest, file-presence, and declared-size validation, but the hosting connector rejected its 1,063,492,539-byte upload against a 536,870,912-byte limit. No version was saved or deployed. The accepted full catalog remains available locally; it has not been replaced by a smaller sample. Cloud delivery needs a separate object-storage path for the catalog assets, or a hosting service that accepts the complete static bundle.

## Distance-cue update — 11 September 2026

The updated point shader and GPU picker passed an isolated actual-GPU check: a nearby point reached alpha 255 and covered more pixels, a middle-distance point reached alpha 149, and a distant point produced neither visible pixels nor pick hits. Disabling the effect restored the distant point and its pick hit. The regular full-catalog interaction checks also passed, including measurement persistence, integrity rejection, and context recovery.

On the same M3 Max/Chrome setup at 1920 × 1080, the updated adaptive overview measured **120.0 FPS, p95 9.3 ms**, with 1,990,709 submitted points and 142 draw calls. Managed allocations were 438.0 MiB. These results remain specific to the measured overview route.

Manual checks passed for clicking a galaxy, focusing to its neighborhood, switching the Navigation Help checkbox off/on, entering pointer-lock flight, and releasing it with Escape. The overview and focused views were visually reviewed. Sustained flight/eviction stress tests remain pending. All seven TypeScript tests and the strict production build passed. Catalog files and scientific computations were unchanged; data validation was not repeated.

The subsequent Observer toolbar button was manually verified in Chrome: clicking it centered the observer marker and changed the overview scale to the local neighborhood scale. The strict production build passed. Observer and selected-galaxy focus share the same camera routine, which clears residual orbit damping and sets the focus distance to 25 Mpc.

## Flight speed controls regression — 11 September 2026

Auto fly was subsequently verified in Chrome: it moved the camera through the cloud without pointer capture, kept the speed slider editable during travel, and stopped on Escape while the slider held keyboard focus. The controls returned to their paused state, preserving the revised speed. The strict production build passed.

Reproduced in Chrome: Fly captured the pointer before the slider appeared, and Escape removed the slider on unlock. The root cause was `flight-controls.hidden = !active` coupling settings visibility to pointer capture. There was no independent settings-open state.

The native-browser regression route now passes: Fly opens an enabled slider without capturing the mouse; a slider change updates the displayed speed; Start flying captures the mouse at that value; Escape leaves the slider visible and enabled; another change persists when flight restarts; Orbit closes the panel. The first configured readout was 26 million ly/sec, and the resumed flight retained the revised 41 million ly/sec. This loop used actual browser pointer lock and accessibility state. No shallow unit test was added for browser pointer capture. Production compilation passed; no debug instrumentation was added.

## NGC 4026 resolved profile — 11 September 2026

The extractor matched the exact DESI target/dense ID and SGA reference to checksum-verified leaf data, retained the original derived distance, and recovered imaging shape/profile fields from the raw source row. The existing 14,140,375-galaxy spatial data was not rebuilt. The Gaussian approximation has 0.054% maximum relative surface-profile error over 0.01–8 effective radii, before display color/exposure and tail taper.

All **12 TypeScript tests passed**, including independent projection-covariance checks at multiple RA/Dec positions and ellipticity signs, angular/comoving scale, integrated half-light radius, transition bounds, selection away from the center, and inside/far model states. Strict TypeScript compilation and the Sites production build passed, including the full catalog asset presence/size checks.

Actual Chrome 152 / WebGL2 / ANGLE Metal checks passed:

- Rendering from the observer direction, with camera zoom sufficient to resolve the image, recovered minor/major ratio **0.314739**, versus the catalog-derived **0.314602**. A face-on view recovered **0.999993**.
- The shader rendered from inside the volume and submitted no visible model when distant. These tests passed again after forced WebGL context loss and recovery.
- Clicking the resolved body two effective radii from its center selected the exact target **39633263488141603** through the normal interaction path.
- Visit galaxy and the close-up inspector were visually reviewed. Distant point clutter fades out, while the observer marker does not appear behind the camera.
- Seven seconds of continuous close-up orbit at a **2268 × 1218** drawing buffer measured **120.0 FPS, p95 9.3 ms** over the final rolling frame window. The view submitted 25,836 catalog points, with **4 total draw calls** (including the one galaxy volume), approximately **61.6 MiB** tracked allocations, and no pending/failed chunks or memory limit.

The final shared-shader regression also passed ordinary GPU point picking, two distinct exact IDs, measurement persistence through detail changes, distance-cue visibility/picking, integrity rejection, and context recovery. The source extractor rerun produced the same profile.

This is one model on the local M3 Max. It does not establish performance for many simultaneous volumes, other GPUs, or Safari/Firefox volume rendering. The measured profile is distinct from the chosen 3D depth and illustrative colors. The existing hosting upload-size blocker is unchanged.

## Spiral preview, name search, and opacity floor — 11 September 2026

Added NGC 3982 as a second measured global profile with illustrative spiral arms; retained the smooth NGC 4026 model. Its source extraction verified the exact DESI/SGA identity and existing metadata, and the 17-Gaussian smooth profile fit has 0.246% maximum relative error. The local search index contains 17,320 names and 3,181 destinations verified against existing leaf checksums and target IDs. Main catalog binaries and cosmological computations were unchanged.

All **17 TypeScript tests passed**, covering projection, scale, model transitions, deterministic bounded arm geometry, normalized catalog/Messier aliases, honest unavailable-name behavior, nearby suggestions, and unique complete visit destinations. Strict compilation and the Sites production build passed, including the full catalog presence/size checks.

Actual Chrome 152 / WebGL2 / M3 Max checks passed:

- Both smooth model components retained their observer-projected axis ratios: **0.314739 vs 0.314602** for NGC 4026 and **0.879160 vs 0.878839** for NGC 3982. Face-on, inside-volume, distant-invisibility, and context-recovery checks passed. Spiral light is explicitly excluded from the smooth-profile measurement because its distribution is illustrative.
- Body selection two effective radii from each center returned the correct, distinct exact DESI target ID.
- At a **1%** distant opacity floor the GPU produced maximum alpha **3/255**, with visible pixels and successful pick coverage. At **0%** the same distant point had neither visible pixels nor pick coverage. Near points remained fully opaque and larger.
- Seven seconds of close-up orbit at **2268 × 1218** measured **120.0 FPS, p95 9.3 ms** with either floor. With 0% the view submitted 25,836 catalog points, **5 draw calls**, and **65.3 MiB** managed allocations. With 2% it retained 1,489,586 submitted points, **87 draw calls**, and **166.7 MiB**. Neither run had pending/failed chunks or hit its memory limit at measurement. These short, local measurements are not guarantees for other GPUs or simultaneous resolved populations.
- The ordinary full-catalog regression passed GPU point picking of two distinct IDs, measurement persistence through detail changes, integrity rejection, distance-fade checks, and context recovery.

Native Chrome UI review confirmed Visit's nearby suggestions, autocomplete for `ngc 03982`, Enter-to-visit for both NGC 3982 and `Messier 109` (NGC 3992), correct selected-name/exact-ID display, and an explicit unavailable explanation for Andromeda. The spiral close-up and its measured-versus-illustrative description were visually reviewed. Settings accepted a **2%** floor, disabled the slider when distance cues were off, and retained the floor after reload. A positive floor keeps more catalog geometry active; the normal adaptive and memory budgets remain in force.

Raw reports are included in [validation-results.json](validation-results.json). Name coverage is deliberately partial; unavailable familiar names do not receive fabricated positions. The previously documented hosting upload-size blocker remains unchanged.

## All-galaxy models — 11 September 2026

The model extractor emitted profiles aligned with all 1,010 existing spatial chunks. The independent model validator checked every compressed SHA-256, binary header/size, unique full leaf coverage, model counts, and exact agreement with the two original measured profiles. **14,140,375 model records** passed: **12,097,577 usable measured shapes**, **2,042,798 assumed shapes**, and **3,128 recorded visual classifications**. Positions, distances, and the original point/metadata binaries were unchanged. The sidecars total **142,725,259 compressed bytes**.

All **22 TypeScript tests passed**. New checks distinguish imaging fits from visual classification, preserve projected covariance for all five 3D families including very flattened shapes, identify missing/PSF shapes as assumptions, reject wrong profile formats, and bound deterministic irregular geometry. The Python checks also guard the meaningful `Sb` versus `SB` distinction. Strict TypeScript and production compilation passed.

Actual Chrome 152 / M3 Max GPU checks passed for NGC 3982 (spiral), NGC 5107 (barred), NGC 4026 (lenticular), NGC 4121 (elliptical), and NGC 3738 (irregular). Every family selected the correct target from its visible body, recovered the measured projected ellipse in the smooth component, rendered from inside, and disappeared at unresolved distances. The unknown-shape example retained its exact DESI identity and disclosed an assumed **0.005 Mpc** radius.

Removing NGC 3738's model, clearing selection, and approaching its location loaded it automatically without selecting it. A batch of **24 real catalog model requests saturated the pool at exactly 12 resident models**, exercising eviction. Distance fading at 0% and 1%, ordinary GPU point selection, measurement persistence, integrity rejection, and WebGL context recovery also passed.

With the saved **2%** opacity floor, a seven-second irregular-galaxy orbit measured approximately **110 FPS, p95 9.3 ms** at a **2268 × 1218** drawing buffer, with two visible models, 83 draw calls, approximately 216.6 MiB tracked allocations, and no failed chunks or memory-limit state. The twenty-second **1920 × 1080** adaptive overview benchmark measured **120.0 FPS, p95 9.3 ms**, with 1,990,709 submitted points and 142 draw calls. Its 556.2 MiB tracked allocations included the complete point catalog retained from the preceding full-detail interaction test. These local short runs do not establish performance on other devices or a long flight through dense clusters.

The barred-spiral, irregular and elliptical close-ups, named model suggestions, and measured-versus-assumed inspector text were visually reviewed in native Chrome. Catalog-wide profiles, name search and original preview URLs now resolve from the selected catalog origin, preparing the app for a separate public data host. Public GitHub and web delivery remain the next release step; no public deployment is claimed by this validation.

## Clear navigation and UI review — 11 September 2026

The initial real-GPU observer approach reproduced a full-screen foreground washout with just NGC 3982: 48.7% bright coverage at six half-light radii, 96.5% at two radii, and 100% at 0.25 radii and the center. Off-center clicks hit the volume. Automatic obstruction fading now records **zero bright pixels and no model body hit** at each of those close approach distances. Deliberate focus retains full model visibility; Observer and Overview remove that exemption while preserving inspection. Points-only and focused-only choices pass, and switching to points keeps the focused neighborhood's distance-fading horizon stable.

The new `?uxtest` route checks the real camera, renderer and UI. Nine control checks pass: initial Focus reachability, Close after scrolling, Retry hit target, delayed Visit cancellation, reusable search, immediate close/reopen cancellation, editable flight speed, auto fly with a free pointer, and Escape preserving the speed menu. Native Chrome review verified autocomplete/Enter-to-visit, restored trigger focus, Settings choices and persistence, and keyboard access to scrolling inspector content at 150% browser zoom (about 1008×541 CSS pixels). Browser zoom and Automatic display were restored afterward.

The combined `?uxtest&modeltest&selftest&run=ux-release-check` also passed all five model families, measured sky projection, inside-volume views, body picking, the assumed-shape example, automatic nearby streaming and the 12-model limit, point picking, distance fading, measurement persistence, integrity rejection and context recovery. **24 TypeScript tests** and the final Sites production build pass. The build verifies the full 14,140,375-record catalog and model coverage; binary source data was unchanged.

The final seven-second close-up orbit on Chrome 152 / M3 Max at **2268×1218** measured **109.5 FPS, p95 8.4 ms**, with three visible models, 90 draw calls, 236.1 MiB tracked allocations and no pending/failed chunks or memory-limit state. This is a short local run, not a sustained dense-cluster or cross-device guarantee. See [the UI review](ux-review.md) and [baseline/final reports](validation-results.json). The previously recorded public-hosting data-size constraint is unchanged; this is local validation.

## Milky Way reference — 11 September 2026

All **27 TypeScript tests** pass, including independent checks of the Galactic-center sky direction, observer distance, solar height in the adopted midplane, handedness of the transformed basis, arrival framing, reference allocation bound and visibility policies. The production build passes and verifies the unchanged full accepted catalog and all profile assets.

The actual Chrome 152 / M3 Max `?uxtest&hometest&modeltest&selftest&run=milky-way-integration` run passed. Observer arrives at 60 kpc from the origin with the Sun at screen center and the Galactic center offset. Reference-body picking opens the home inspector without replacing the catalog identity or measurement. GPU readback finds visible light outside, near the Sun and from inside the Galaxy; points-only and nonfocused suppression produce no light. The Sun marker stays within the clipping range at the 10 pc minimum orbit distance. Every check passed again after forced graphics loss/recovery.

The same run passed all five catalog model families, automatic nearby loading, the 12-model catalog pool, ordinary GPU point picking, distance cues, measurement persistence, integrity rejection, the observer obstruction regression and all nine UI checks. The home model is separately bounded at **1,792,000 tracked geometry bytes**. Its seven-second orbit at **2268×1218** measured **109.1 FPS, p95 9.4 ms**, 19 total draw calls, one visible home model, 124.9 MiB tracked allocations, and no pending/failed chunks or memory-limit state. These are short local measurements, not hardware-wide performance guarantees.

The Observer arrival and reference inspector were visually reviewed in native Chrome. Searching `Milky Way` and pressing Enter opened the reference; **Galaxy view** centered the Galaxy with the Sun visibly offset, and **Our position** restored the Sun-centered view. Source parameters, frame transformation and scientific/visual assumptions are recorded in [milky-way.md](milky-way.md).

## README clean-checkout verification — 11 September 2026

The documented quick start was executed in an isolated checkout without copying the existing catalog binaries: `npm ci`, `uv sync --frozen`, `npm run data:bootstrap`, and `npm run dev`. Bootstrap downloaded 24 byte ranges from the official DESI source and produced **47,589 accepted observations** in one node, about **2.9 MiB compressed**. The main workspace's full catalog was untouched.

All **27 TypeScript tests**, the **four Python unit checks**, independent validation of the generated bootstrap dataset, and `npm run build` passed. The independent validator confirmed all 47,589 records with maximum reconstructed render-position error **0.00024372 Mpc**. HTTP checks verified that the active manifest and every declared point/metadata asset were served with the expected byte lengths.

The fresh setup's actual Chrome 152 / M3 Max `?hometest&selftest&run=readme-bootstrap` run passed Milky Way arrival, body picking, rendering modes, near-Sun clipping and graphics recovery; point picking returned two distinct exact IDs and their **589.007162 Mpc** separation. Measurement persistence, distance cues, integrity rejection and ordinary graphics recovery also passed. The combined runner resets to the survey overview before point checks so a preceding home test does not leave distant points intentionally faded out. Raw results are retained in [validation-results.json](validation-results.json).

## Galactic-core focus and optional point enlargement — 11 September 2026

The baseline `home-framing-before` reproduced the user's offset: Observer and Milky Way search zoomed toward the Sun, growing the core's offset from **104.9 to 290.2 CSS pixels** between camera distances of 60 and 19.8 kpc. The preexisting Galaxy view stayed centered through the same wheel events. Physical placement/size tests passed; the problem was the destination's camera target.

The combined `?uxtest&hometest&modeltest&selftest&run=home-core-point-size` run passes. The renamed Milky Way toolbar action, Galactic core inspector action and Milky Way autocomplete/Enter path all keep the core within **0.001 CSS pixels** of center through the same zoom. Sun / Observer still centers the origin. Both focus-state indicators and the separate core/Sun annotations pass their real-UI checks. Full model rendering, body/point picking, obstruction suppression, delayed-search cancellation, measurements, integrity rejection and graphics recovery remain passing.

The new independent size setting is off by default. With an 8-pixel diagnostic marker, GPU readback measures **52 covered pixels off / 124 on**, both with fully opaque near centers. The ID pass measures **52 off / 140 on**; distant floor visibility/picking still passes. Size changes also work with distance fading disabled. The real settings handler changes the renderer and saves both values. Native Chrome review verified the settings layout and reload persistence with enlargement both on and off; enlargement was left off for testing. The existing 2% opacity floor and Automatic model display were preserved.

All **27 TypeScript tests** and the final Sites production build pass, including the full accepted catalog/model asset checks. The seven-second home orbit on Chrome 152 / M3 Max at **2268×1218** measured **108.3 FPS, p95 9.3 ms**, 20 draw calls and 124.9 MiB tracked allocations, with no pending/failed chunks or memory-limit state. These are short local measurements. No catalog positions, reference geometry, model dimensions or source data changed. Baseline and final browser reports are in [validation-results.json](validation-results.json).

## Search availability clarity — 11 September 2026

The `search-availability-before` real-DOM regression reproduced a disabled Andromeda row being counted and highlighted as a visit suggestion; selecting it replaced the count with a missing-observation message. Independent alias tests and the source name index confirmed correct recognition of NGC 224 without a verified DESI destination.

`?uxtest&hometest&run=search-availability-fixed` passes the new availability regression. Andromeda has one recognized name, a readable location-unavailable explanation, zero visit options and no active keyboard option. Clicking or pressing Enter preserves both message and camera; Browse available galaxies restores usable suggestions and input focus. Mixed queries exclude unavailable names from keyboard selection, and an unknown query clears stale content and uses a distinct empty-state message. Existing cancellation, reopen, flight, obstruction, Milky Way search/zoom, reference rendering and context recovery checks all pass.

Native Chrome review confirmed the Andromeda panel, aliases, recovery action and the mixed `NGC 398` result count. All **27 TypeScript tests** and the final Sites production build pass, including the unchanged 14,140,375-record catalog and model asset verification. No new destinations or astronomical measurements were added. Baseline and final reports are retained in [validation-results.json](validation-results.json).

## Embedded local positions — 11 September 2026

The baseline `local-positions-before` and fixed `local-distance-guard` reports preserve the actual-GPU regression for the 1,107 metadata positions inside the Milky Way model's outer sphere. Across four orbit angles, protected visual and ID coverage changes from nonzero to **zero**. Disabling fading and setting a 100% floor do not bypass the safeguard. Raw mode shows amber points; a control beyond the guard remains visible and pickable.

The real-chunk/settings test exercises node 701 with a nonzero chunk origin and target `39628351635193964`. Raw ID readback equals the expected code **46006272**. Hidden visits are rejected; raw visits retain identity and never create physical models. Settings persistence, uncertainty notices, annotation suppression and preserved selected records/measurement endpoints/catalog count all pass. The full-data audit probes report unavailable for subsets rather than failing the independent reference tests.

The combined `?uxtest&hometest&modeltest&selftest&run=local-distance-guard` passes the new local-position checks and all existing UI/search, core/Sun navigation, Milky Way rendering/recovery, five catalog families, automatic streaming, 12-model bound, point settings, exact-ID picking, measurement persistence, integrity rejection and context recovery. **27 TypeScript tests** and the Sites production build pass; asset checks confirm all **14,140,375 observations** and profile coverage. No prepared catalog asset changed.

Native Chrome review confirmed the raw amber cloud, its removal, the new setting and its persistence after reload. The protected core view was restored afterward with the user's 2% opacity floor and enlargement disabled. In this combined run at 2268×1218 on Chrome 152 / M3 Max, the short home orbit recorded **101.4 FPS, p95 25 ms**, and the model orbit **107.5 FPS, p95 9.2 ms**. The home p95 exceeds the 20 ms target; these short combined acceptance samples are not a sustained performance benchmark or a cross-device guarantee. Both snapshots had no failed/pending chunks or memory-limit state.

The policy hides uncertain local positions without altering their original numerical distances. The 1 Mpc radius is a display safeguard, not a scientific certainty boundary. [Audit and source context](local-distance-audit.md); [raw reports](validation-results.json).

## Independent nearby galaxies — 11 September 2026

The six nearby entries pass **30 TypeScript tests**, including sourced-distance retention, separate IDs/null redshifts, coordinate conversion, independent-distance guard bypass, projected ellipse preservation, bounded geometry and alias deduplication. Regenerating the reference from its pinned source excerpt produces byte-for-byte identical output. The final Sites production build passes and verifies the unchanged **14,140,375 accepted observations** and all DESI model assets.

The actual Chrome 152 / M3 Max full-data `?nearbytest&uxtest&hometest&modeltest&selftest&run=nearby-integration` suite passes. All six names resolve to usable destinations; their visits center the correct model, show independent-distance provenance without redshift fields, preserve projected shapes, and support body and GPU point selection under their separate identities. The same checks pass after forced graphics loss/recovery. M31–M33 separation is **0.206452254649 Mpc** and remains unchanged, with visible endpoints, when the uncertain-local-position setting changes. Original DESI counts stay unchanged. The nearby models add **2,688,000 tracked geometry bytes**.

The full run also passes the prior availability/late-search cancellation, flight controls, foreground obstruction, Galactic-core wheel navigation, Milky Way reference rendering, embedded-redshift-position safeguard, five DESI model families, 12-model DESI bound, point settings, ordinary selection/measurement, integrity rejection and context recovery checks. At **2268×1218**, the short home orbit recorded **99.7 FPS, p95 9.4 ms**, with 25 draw calls and 157.7 MiB tracked allocations; the model orbit recorded **104.7 FPS, p95 8.7 ms**, with 89 calls and 276.5 MiB. Both had no failed/pending chunks or memory-limit state. These are short local acceptance samples, not a dedicated nearby fly-through benchmark or a cross-device guarantee.

The million-object `?dataset=development&nearbytest&hometest&selftest&run=nearby-subset` suite also passes all six local destinations, model/point picking, measurement preservation, source UI, home navigation, point settings and graphics recovery. DESI-specific embedded-position probes correctly report unavailable on subsets. This exercises the subset branch that retains the nearby layer without loading the full DESI name index; a fresh bootstrap setup was not repeated for this feature.

Native Chrome visual review confirmed Andromeda's single available search result and Enter-to-visit, centered spiral model, source links and quoted distance error. LMC and SMC show the assumed radius labels; LMC shows unknown orientation while SMC retains the adopted 45° sky angle. The full atlas was left on Andromeda with Automatic models, uncertainty protection enabled, a 2% distant-opacity floor and point enlargement off. Final subsequent source edits only clarified About/Help copy and the search placeholder; the native review includes those edits. [Raw browser reports](validation-results.json); [measurements and assumptions](nearby-galaxies.md).

## All spirals and continuous model residency — 11 September 2026

The actual-GPU baseline replayed two orbits around Andromeda and recorded **26 abrupt central-viewport transitions**, with a peak blend jump of **1.0**. Targeted removal traces confirmed two causes: visible models lost candidate membership as the point frontier changed, and near-equal pool rankings caused immediate replacements. Extending the distance threshold alone would not fix these removals.

`?continuitytest&uxtest&nearbytest&hometest&modeltest&selftest&run=spiral-final` passes on Chrome 152 / Apple M3 Max at **2268×1218**. The same replay now has **zero abrupt transitions**; peak blend change per sampled step is **0.1541**, all 136 removals occur at zero blend, and residency stays at **12 DESI models**. Pending fades finish after the camera stops. The threshold for a reported abrupt transition is 0.35 between approximately 40 ms samples with the center inside the central 85% of the viewport. These checks isolate interior model popping; ordinary field-of-view exits and subthreshold fades are expected.

The real appearance setting switches both catalog and nearby models, saves the preference, preserves source objects/identities/positions and catalog totals, retains body picking, and shows the override disclosure. The default all-spiral nearby geometry is **4,032,000 tracked bytes**. All existing foreground-obstruction, search lifecycle, flight controls, Milky Way core/wheel focus, uncertain-local protection, named/source families, 12-model saturation, projected-profile checks, body/point selection, measurement persistence, integrity rejection and graphics recovery pass. Catalog source profiles are still independently tested even when the rendered appearance is overridden.

The development-subset `?dataset=development&continuitytest&nearbytest&selftest&run=spiral-subset` also passes appearance switching, six nearby destinations, source UI, model/point picking, measurements, point settings and recovery. Its DESI orbit probe reports unavailable as intended. The final idle-loop adjustment affects streamed DESI residency; it was included in the subsequent full acceptance run.

All **31 TypeScript tests** and the production build pass; asset checks confirm the unchanged **14,140,375 accepted observations** and complete DESI profiles. New tests verify spiral geometry across all source families without rewriting data or projected covariance. The standalone illustrative disk profile is reproducibly generated from an exponential law, with no catalog mutation. Native Chrome review checked the appearance menu, both options, persistence after reload, the inspector disclosure and Andromeda framing. Automatic display and All spirals were left enabled; existing opacity and local-uncertainty preferences were preserved.

The final short model orbit recorded **97.3 FPS, p95 9.4 ms**, 87 draw calls and 396.5 MiB tracked allocations. The home orbit recorded **120.0 FPS, p95 9.2 ms**, 24 calls and 330.9 MiB. Both had no pending/failed chunks or memory-limit state. These are short local acceptance samples with warm streamed data, not sustained or cross-device guarantees. [Baseline, targeted trace and final reports](validation-results.json).

## Subtle galaxy color variation — 11 September 2026

All **33 TypeScript tests** and the final production build pass, including unchanged coverage of **14,140,375 accepted observations** and their model assets. Palette tests cover deterministic identity-based colors, restrained channel ranges, controlled luminance, and unchanged arm/clump positions and sizes.

The actual-GPU `?colortest&continuitytest&uxtest&nearbytest&hometest&modeltest&selftest&run=galaxy-colors` completes with all 79 reported pass/recovery/integrity assertions true. The isolated color comparison renders identical spiral geometry with two nearby identities: aggregate red/blue ratios are **0.689** and **1.134**, while integrated luminance differs by only **0.0151%**. Recreating the same identity produces an identical pixel checksum, including after graphics-context recovery. Both variants retain **2 draw calls and 672,000 geometry bytes**.

The two Andromeda orbits retain **zero abrupt central-viewport jumps**, peak sample blend change **0.139**, **130 removals all at zero blend**, and the **12-model resident cap**. Fades finish after movement stops. Existing nearby search/framing/picking, source preservation, Milky Way core/Sun navigation, local-position safeguards, profile projection, point settings and measurement checks pass. The separate million-record development-subset `?colortest&nearbytest&selftest&run=galaxy-colors-subset` completes with all 54 reported assertions true.

Short local orbit samples on Chrome 152 / M3 Max at **2268×1218** measure **98.6 FPS, p95 9.3 ms** for the model route and **120.0 FPS, p95 9.3 ms** for the Milky Way route. Both have no pending/failed chunks or memory-limit state. These are local acceptance measurements, not cross-device performance guarantees. Raw reports are retained in [validation-results.json](validation-results.json).

Native Chrome review confirmed the blue-white Andromeda palette and the soft ivory Small Magellanic Cloud palette, both with restrained warm centers and preserved spiral structure. The inspector explains that the varying colors are illustrative. The full atlas was returned to Andromeda afterward.

## Related disk variants — 11 September 2026

Two new default-appearance recipes share the common spiral template: a finer four-arm disk and a ringed spiral disk. All **36 TypeScript tests** and the final production build pass, including unchanged **14,140,375-record** coverage and model-asset checks. Tests independently compare radial positions, heights, point sizes/colors, identity stability across row remapping, source preservation, common exposure and sample budgets.

The actual-GPU `galaxy-variants-release` suite passes all 82 reported pass/recovery/integrity assertions. With identical seed, palette and dimensions at face-on, 60° and 85° inclinations, the new recipes' integrated light ranges from **99.988% to 100.415%** of the classic template. Peak luminance never exceeds the corresponding classic view, with no increase in near-white pixel count. Each comparison uses **2 draw calls and 672,000 tracked geometry bytes**, remains body-pickable, reproduces its pixel checksum when reconstructed, and passes again after graphics-context recovery.

A trial barred recipe was excluded: despite similar integrated light, its local peaks and near-edge-on highlights exceeded the comparison limits. The first trial report is retained as evidence that total brightness alone is insufficient. Native review also led to retaining half the ringed recipe's particles on their spiral paths, keeping the ring as an accent. The accepted recipes do not add any central component or change the common smooth profile.

The full release suite passes color stability, appearance/source preservation, six nearby destinations and measurements, UI/search/flight controls, Milky Way navigation/rendering, local-position safeguards, catalog profiles, point picking/settings and graphics recovery. Its Andromeda replay records **zero abrupt transitions**, **136 removals all at zero blend**, peak sampled blend change **0.153**, the **12-model cap**, and completed idle fades. The initial developer preview intercepted pointer hit-testing in the UI reachability probe; it now ignores pointer events like the existing diagnostic panel. `galaxy-variants-ui` separately passes all nine UI checks with the preview visible. Earlier reports are retained rather than rewritten.

Short local route samples on Chrome 152 / M3 Max at **2268×1218** measure **104.3 FPS, p95 9.0 ms** for the model route and **120.0 FPS, p95 9.0 ms** for the Milky Way route. Both have no pending/failed chunks or memory-limit state. These are local acceptance measurements, not sustained or cross-device guarantees. [Raw reports](validation-results.json).

The final million-record development-subset `galaxy-variants-release-subset` run completes with all **57 reported assertions passing**, including variant/color recovery and all six nearby destinations. Nearby geometry remains **4,032,000 tracked bytes** and its count remains separate. Native Chrome side-by-side review confirms the final ring accent retains recognizable spiral arms.

## Public release preparation — 2026-09-11

The isolated Pages build validates all full-catalog point/metadata/profile SHA-256 values and matching source identities before copying an explicit asset allowlist. It includes all 14,140,375 observations, search/profile sidecars and unchanged attribution/license files. Output: 3,052 files, 1,078.4 MiB, maximum file 3.24 MiB; within documented Pages Wrangler limits of 20,000 files and 25 MiB per file. Development subsets and private hosting configuration are excluded; the original local catalog remains intact.

All 39 TypeScript tests pass, including release path-traversal rejection, relative versioned-data resolution and host-limit failures. The full model validator passes all 14,140,375 records, hashes and leaf coverage. Strict TypeScript and the production Pages build pass. Native Chrome preview verified full-count overview rendering and NGC 3982 search/arrival, measured-shape inspector and spiral rendering from the new paths. This change does not alter shaders, model geometry or camera behavior; no new performance claim is made.

Original data credits, third-party notices, license text and source sidecars are unchanged by MIT adoption and copied verbatim into the release. A targeted tracked-file/history audit checks private hosting IDs, config paths and recognized credential patterns. An obsolete hosting file was removed from all 27 prior commits with a private local backup; existing development history was preserved with new commit hashes. Live deployment URLs and operational verification receipts are intentionally kept only in ignored `.deploy/`.

A fresh public GitHub clone also completed the README quick start: `npm ci`, `uv sync --frozen`, bootstrap preparation (47,589 real observations) and ordinary `npm run build`, with no private hosting files. Three 1920×1080 page captures were visually reviewed for the README; they use actual app navigation and retain scientific disclosures. The reported stationary native cursor overlay persists on Chrome New Tab and is absent from direct page captures, so no application cursor fix was made.

The complete Pages deployment passed live HTTPS verification: exact app/font/license assets and data sidecars, matching catalog counts, nine sampled point/metadata/profile chunks with SHA-256 and decoded-format checks, correct binary MIME types, and real 404 responses for private configuration paths. A separate Chrome run verified overview rendering, NGC 3982 and Andromeda search/model visits, Milky Way navigation and the data-attribution link with no page errors or failed HTTP responses. Live screenshots were visually reviewed. All local catalog binaries were checked during packaging; hosted binary checks are a representative sample, not a full re-download. Detailed deployment and browser receipts remain private in `.deploy/`.

## Continuous Milky Way starlight and dust — 11 September 2026

All **40 repository tests** pass (`npx vitest run --exclude '.cache/**'`; the ignored fresh-clone fixture contains another copy of the older tests). The reproducible reference generator preserves every adopted coordinate, axis, dimension and frame citation exactly. The full Pages production build validates the unchanged 14,140,375-observation catalog and asset hashes. Additional appearance citations are appended to acknowledgments; existing data licenses and credits remain intact.

The full-data `?hometest&uxtest&continuitytest` and development-subset `?dataset=development&hometest` suites pass on Chrome / **ANGLE Metal, Apple M3 Max**, with no JavaScript or shader errors. Home wheel/core/Sun focus, search, picking, local-position safeguards, display choices, inside-disk rendering and context recovery pass. The isolated appearance probe passes before and after recovery: face-on, inclined, reverse and edge-on images each use **one draw call**, with **zero pixels at or above 250/255**. Peak channels across those views range from 217 to 236. Repeated frames are byte-identical. A 0.001-radian orbit change has a mean RGB difference of 0.034/255; disabling dust changes the inclined image by 1.75/255 and increases its mean luminance. These are renderer regression checks, not scientific validation of the illustrative dust distribution.

The density field and all mip levels account for **2,446,676 CPU/GPU bytes** (2.33 MiB), excluding driver/program overhead. The short full-app home orbit at **1600×1000** records **60.0 FPS, p95 16.7 ms**, 10 total scene draws, no failed/pending chunks and no memory-limit state. This browser session was paced at 60 Hz; the result is not a cross-device or maximum-throughput claim. Catalog appearance, the 12-model cap and continuous residency checks also pass. The existing synthetic Window key event revealed an unsafe `target.matches` call; guarding for an Element removes the exception, and the UI suite passes without console errors.

Native Chrome production review and direct page captures checked the new source disclosure, core/Sun actions, real wheel/orbit controls and the updated README image. Isolated GPU views were also inspected face-on, inclined, edge-on and from the reverse side. Texture filtering includes the ray-cell footprint to suppress steep-angle aliasing. The final production preview has no page or shader errors. [Full and subset reports](validation-results.json); [appearance choices and sources](milky-way.md).

After deployment, live HTTPS checks pass for the new app/attribution assets, unchanged complete catalog, source sidecars and sampled binary hashes. A separate live Chrome check verifies the updated appearance link, core/Sun actions, zoom, orbit and model screenshot without JavaScript or shader errors. Hosting addresses and operational receipts remain in ignored local deployment files.

## README gallery refresh — 11 September 2026

Recaptured four 1920×1080 images from the current production build: cosmic web, Milky Way, an additional inclined Milky Way view, and Andromeda. Each uses real search/navigation controls and was visually inspected for framing, visible source disclosures and clean rendering. The capture session reports no JavaScript or shader errors. README image paths, JPEG dimensions and existing CC BY-SA credits were checked before committing. This update changes documentation and screenshots only; the application and deployed build are unchanged.

## Experimental CMB shell — 12 September 2026

All **42 repository tests** pass (`npm test -- --exclude '.cache/**'`, excluding the ignored clone fixture). The Pages production build validates every full-data hash and retains **14,140,375 observations**. The sourced reference generator yields a Planck18 comoving radius of **13,884.421 Mpc** at rounded z = 1090; tests distinguish this radius from lookback time and verify linear catalog reach. See [scientific scope and sources](cosmic-horizon.md).

Full-data `?cosmictest&uxtest&hometest&continuitytest&selftest` and development-subset `?dataset=development&cosmictest&selftest` pass on Chrome 152 / **ANGLE Metal, Apple M3 Max**, at **1600×1000**, with the shell enabled throughout shared checks. There are no JavaScript/shader errors. The shell uses one draw and 36 bytes of geometry; disabled rendering uses zero draws. Exterior silhouette tests leave corners clear; outward-facing rays behind the sphere draw nothing. Observer, inside, boundary and just-outside views cover the full test target without clipping, and crossing the shell preserves the restrained brightness. Repeated samples and samples after context recovery have matching checksums. Existing camera/search lifecycle, model continuity, point/body picking, measurements, local-position protections, Milky Way navigation/appearance and graphics recovery pass.

The real production controls pass off-by-default behavior, toolbar/Settings synchronization, both saved states after reload, unit switching, inspector preservation on toggle, and explicit cosmic-scale framing from Settings. Actual production screenshots were inspected at desktop, short-window and narrow-window sizes. The final compact layout also passes frame-action and every toolbar-button hit test at **1000×600** and **390×844**, with observer labels behind the card and readable comparison text. Both the complete production control pass and a fresh full-data package build were repeated after those CSS changes. The short GPU home route measures **60.0 FPS, p95 16.8 ms**, with 11 total scene draws, no failed/pending chunks and no budget-limit state; this is a local 60 Hz acceptance sample, not a cross-device guarantee.

Final acceptance: full and subset GPU reports and the production control/layout report are retained in [validation-results.json](validation-results.json). All sampled shell checksums match before/after context restoration. The public-source/history audit passes.

## Lookback time — 12 September 2026

All **52 repository tests** pass (91 with `npm test`, which currently also collects the ignored `.cache` clone; ten are new). Strict TypeScript and `npm run build` pass. `prepare_lookback.py` records a maximum comoving→lookback interpolation error of **1.1 × 10⁻³ Gyr** where the lookback–comoving curve bends most, around z ≈ 1.5 (about 4,500 Mpc), measured deterministically at every row midpoint plus the rings and random redshifts; every ring round-trips within that budget (the 10 Gyr ring errs 1.0 × 10⁻³ Gyr, the 13 and 13.5 Gyr rings under 2 × 10⁻⁴) and the sub-0.44 Mpc branch reproduces distance ÷ c (about 26,500 years at the adopted Galactic-center distance).

Full-data `?lookbacktest&cosmictest&hometest&selftest` and development-subset `?dataset=development&lookbacktest` pass on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max** at **1600×1000**, with rings enabled from the saved setting and no JavaScript or shader errors. A complete overview frame uses **75 draws** with rings off and **76** with rings on (65 and 66 on the subset), returning to 75 (65) when disabled again. The isolated 256×256 overview render uses one draw, lights **6,844** pixels (6,800 on the subset) with a peak channel of 89/255, and repeats the same checksum (2349650824 full, 368742171 subset) immediately and after WebGL context loss and recovery. A camera 10 pc from the observer yields an empty ring set and zero draws; disabled rendering draws and lights nothing. Eight rings (7–13.5 Gyr) are chosen at the overview with five visible, non-overlapping labels that disappear when disabled. Camera pose, selection, catalog count and both saved states are preserved; the table is monotonic; ring geometry is 36 bytes. The CMB shell, home navigation and appearance, selection, measurement and integrity checks pass alongside. The short home orbit records **60.0 FPS, p95 16.7 ms** with 10 scene draws and no failed, pending or budget-limited state; this is a local 60 Hz sample, not a cross-device guarantee. Reports `lookback-full` and `lookback-subset` are in [validation-results.json](validation-results.json).

## Survey footprint — 12 September 2026

All tracked vitest suites pass (**74 tests** at this commit, three of them new for the footprint: sidecar decoding, the shader's direction→cell mapping against three known cells, and mismatch rejection); `npm test` also collects the ignored `.cache` clone. Strict TypeScript, `npm run build` and `npm run test:data` pass; the Python footprint check re-bins the largest leaf independently and was confirmed to fail on a Dec-flipped, RA-flipped or shifted grid. `prepare_survey_footprint.py` bins all **14,140,375** accepted rows from the 882 checksum-verified leaf chunks in about 5 s: **69,895 occupied 0.5° cells** (33.6% of the sky weighted by cos Dec), densest cell **2,810 rows** at RA 149.75° Dec +1.25° (the COSMOS field), a 346,191-byte sidecar that is byte-identical across runs. The release planner accepts it under the shared source-hash and count rule (3,040 files).

Refactoring the CMB shell, lookback rings and footprint onto `screen-overlay.ts` was verified first: `?cosmictest&lookbacktest` (`overlay-helper`) repeats the lookback overview checksum 2349650824 with 6,844 lit pixels and 75/76/75 frame draws, and every CMB probe checksum recorded for `cosmic-horizon-full`.

Full-data `?footprinttest&cosmictest&lookbacktest&selftest` and development-subset `?dataset=development&footprinttest` pass on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max** at **1600×1000**, with the footprint enabled from the saved setting and no JavaScript or shader errors. A complete overview frame uses **75 draws** with the footprint off and **76** on (65 and 66 on the subset), returning to 75 (65) when disabled again; with the footprint on, the ring suite still adds exactly one draw (76→77). Isolated 256×256 renders from the observer use one draw each: toward the densest cell the centre alpha is **76/255** (45,300 lit pixels, peak 76), toward the surveyed cell beside a real gap (RA 180.75° Dec +30.25°) it is 27, and toward RA 60° Dec −70° nothing is lit. The COSMOS checksum **1961411294** repeats immediately, after WebGL context loss and recovery, and on the subset. Disabled renders draw and light nothing. A fresh instance pointed at a missing sidecar ends in the failed state with zero draws and 36 bytes. The shell radius equals the manifest's maximum distance (4,830.889 Mpc full, 4,830.852 subset) and memory accounting reports **259,236 bytes** when loaded. Camera pose, selection, catalog count and both saved states are preserved. The CMB shell probe repeats its recorded checksums, the lookback overview repeats 2349650824 with 6,844 lit pixels, and selection, measurement and integrity checks pass alongside. Two probe corrections came out of these runs: frame draw counts are now compared only after two consecutive settled frames (a stacked run had taken its baseline mid-stream, so counts climbed 18→33→39 while every render check passed), and the camera-angle tolerance is a microradian because `Quaternion.angleTo` cannot resolve below about 3 × 10⁻⁸ rad near identity (the subset run reported 2.98 × 10⁻⁸ with position and focus deltas of 10⁻¹¹ Mpc). This is a local acceptance sample on one GPU, not a cross-device guarantee. Reports `footprint-full` and `footprint-subset` are in [validation-results.json](validation-results.json).

## Shareable views and camera travel — 12 September 2026

All tracked repository tests pass (`npx vitest run --dir tests`, 82 across 17 files at 908d49b; 13 are new). The travel tests pin the smoothstep ease at 0.15625 for s = 0.25, the log-space midpoint √(0.003 × 30000), the 13.01 Mpc apex of a 10 Mpc hop (also when the output pose aliases the start pose), constant angular speed along the great circle, antiparallel directions on four axes, and clamping outside [0, 1]; each was confirmed to fail under a hand mutation (linear ease, normalized lerp, linear distance, hop measured after the target write, 1.2 hop factor). The link tests round-trip every identity kind, keep 11-digit camera offsets at a 4,800 Mpc target within 1e-9 relative (which fails with absolute camera encoding or fewer than 12 digits), keep 20-digit and negative target IDs as exact strings, refuse to encode NaN, Infinity or values at 1e6 Mpc, and reject 22 malformed hashes plus the 21-digit id, including a six-digit row, a seven-digit node, a 17-character nearby key, an uppercase key and the retired five-segment `desi` form. Strict TypeScript and `npm run build` pass.

The instant camera paths were regressed before any new behaviour was wired: full-data `?uxtest&hometest&nearbytest&continuitytest&cosmictest` (`travel-regress`) passes every probe on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max** at **1600×1000** with no page errors, the Andromeda replay recording zero abrupt transitions and the 12-model cap, and the home orbit at 59.8 FPS / p95 16.8 ms with 12 draws.

Full-data `?sharetest&uxtest&hometest&nearbytest&continuitytest` (`share-full`) passes with zero page errors and zero failed keys, before and after WebGL context loss and recovery. Encoding the live overview camera and decoding it back reproduces target and camera within **2.8 × 10⁻¹²** and **1.6 × 10⁻¹²** relative (12 significant digits). Links for NGC 3982 (`desi:512:20296:39633325333155389`), the resident root record, a leaf whose dense id had to be loaded from its points chunk, `nearby:m31` and `core` land within **3 × 10⁻¹⁶**, 7 × 10⁻¹⁷, 1.2 × 10⁻¹⁷, 0 and 7 × 10⁻¹⁶ relative of the exact position plus the link's camera offset, each with the expected identity reported back (NGC 3982 also has its model resident and centred, and `core` selects the home panel in its Galactic-core view). Every identity case had its absolute target perturbed by 1 µMpc so only the re-verified position could satisfy the tolerance. A wrong target ID and a row beyond the chunk both leave the selection empty, focus the camera alone at the link's own target (1.28 × 10⁻⁹ relative, the perturbation) and show "This link points to a galaxy this catalog does not contain."; a six-digit row decodes to null. Through the real dialog, saving the Andromeda view stores `{version:1, views:[…]}` with one entry and the placeholder "Andromeda · h:mm", reopening lists it, Open closes the dialog and arrives within 1e-9 relative after the 1.5 s travel, and Delete leaves an empty list and the empty-state note. A real wheel event 0.6 s into a 2 s Milky Way travel resolves it false **925 Mpc** short of the core with **0 Mpc** target drift over the next 300 ms; an uninterrupted 1 s travel ends within **3.4 × 10⁻¹⁶** relative of the instant pose. A 1 s `visitCatalog` to the root record resolves true only once the target is within 7 × 10⁻¹⁷ of the record, and `stopTravel()` 0.5 s into a 2 s travel resolves it false with the camera left where it was (2 × 10⁻¹² Mpc of float wobble at 925 Mpc, judged relative like every pose check, because OrbitControls re-derives the target through `clampLength` each frame). During a 2 s hop from the Milky Way to NGC 3982 the DESI pool stayed within the **12**-model limit (peaks of 6 and 7 across the two passes; 12 and 10 removals, all at zero blend). The shared UI, search, nearby, home and continuity probes pass alongside; the home orbit records about 56 FPS / p95 16.8 ms with 12 draws (a local 60 Hz sample, not a cross-device guarantee).

Development-subset `?dataset=development&sharetest&nearbytest` (`share-subset`) passes the same checks with no page errors: round trip within 3.4 × 10⁻¹² and 7.3 × 10⁻¹³, the resident record, a non-resident leaf (its dense id loaded from the chunk), Andromeda and the core within the same tolerances, both fallbacks with the toast, the six-digit rejection, saved views through the dialog, the wheel cancel 914 Mpc short with zero drift, the exact arrival, the `visitCatalog` arrival and `stopTravel`, all repeated after context recovery; the hop check is trivially bounded there because the subset ships no model catalog. Run hygiene: Vite reloads the page whenever anyone commits to `src/`, so each run was bracketed with `git log -1`; the final full and subset runs both started and ended on eb340b4, and the earlier full run at 14:45 had no commit inside its window. Two earlier subset attempts timed out at 420 s without producing a report (one overlapped this checkout's own edit to `app.ts`, which reloads the page mid-run; the other had no identifiable reload), and a later full run failed only the after-recovery `stopTravel` drift check with the original absolute 1e-12 Mpc tolerance, which the relative tolerance replaced before the final runs. Reports `travel-regress`, `share-full` and `share-subset` are in [validation-results.json](validation-results.json).

The consolidated review fixes (a stale-serial guard in the link fallback, the hidden-record check moved ahead of selection, shared metadata caching, clipboard errors routed to the visible fallback) were re-verified with full-data `?sharetest` (`share-fixes`, HEAD 83835c1 before and after the run, no page errors): every case passes before and after context recovery, including the new hidden uncertain-local record case, in which a 0.10 Mpc redshift-only record (row 0 of node 701) is not selected by its link, the camera lands on the link's own target 1.0 µMpc from the record, and the existing "hidden" notice appears. The hop peaked at 7 and 6 resident models across the two passes with 16 and 10 removals, all at zero blend. That report is also in [validation-results.json](validation-results.json).

## Guided tours — 12 September 2026

At the original tour checkpoint, all **84** tracked repository tests passed (`npx vitest run --dir tests`; 19 concern the tours: 8 pin the route data to its sources and 11 drive the runner with a fake atlas under fake timers). Commit `5a6cfda` subsequently scoped `npm test` to `tests/`, excluding the ignored `.cache` clone. Strict TypeScript and `npm run build` pass. Route-data mutations (a caption lookback drift, a too-small Local Group framing, a shifted cluster position, a member count under 200) and runner mutations (pause keeping its timer, a cancelled travel counted as arrival, the shell not lowered on leaving) each fail exactly one test.

Full-data `?tourtest&uxtest&hometest&nearbytest&cosmictest` (`tour-full`) passes on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max** at **1600×1000** with no page errors, before and after WebGL context loss and recovery, bracketed by `git log -1` on 2178c26 at both ends. At 10× speed the zoom-out arrives at all five stops with the panel showing Arrived: the orbit target matches the Sun, the Galactic core, the stored Local Group midpoint, the overview target and the origin within **5.4 × 10⁻²⁰ Mpc** (exactly zero at four stops), and the camera distance matches 0.003 Mpc, 0.06 Mpc, 1.2 Mpc, `reset`'s 2.1 × root half-diagonal and `viewCosmicHorizon`'s radius / sin(half field) × 1.14 within **9.0 × 10⁻¹⁶** relative. The CMB shell is off at the first four stops and on at the fifth. With the shell saved off, Finished keeps it on until Escape exits, after which the panel is hidden, the shell and its checkbox are off and `atlas-cosmic-horizon` still reads the saved value; with the shell saved on, the CMB stop shows it, Prev back to the overview leaves it on, and Escape leaves it on and saved. A real wheel event during the hop to the Milky Way pauses the tour with the status "Paused · drag or scroll moved the view", Play unpressed, **0 Mpc** target drift and 4.5 × 10⁻¹⁵ relative distance drift over the following 300 ms. The road trip reaches Finished with all **10** stops arrived and nothing skipped. Across both routes the DESI pool peaked at **12** resident models with 210 removals (172 after recovery), none of them an on-screen model above 0.05 blend. The shared CMB, UI, search, nearby, local-position, home navigation and home appearance probes pass alongside; the home orbit records **56.0 FPS, p95 16.8 ms** with 12 draws (a local 60 Hz sample, not a cross-device guarantee).

Development-subset `?dataset=development&tourtest&nearbytest` (`tour-subset`) passes the same checks with no page errors, before and after recovery, on the same commit: the five zoom-out framings within 5.8 × 10⁻¹⁶ relative, both shell states, the wheel pause with zero drift, Escape, and the road trip finishing with stops 1–6, 9 and 10 arrived while NGC 3982 and NGC 4026 are skipped with "… is not available in this dataset; skipping." toasts; the subset ships no model catalog, so the pool checks are trivially bounded at zero. Screenshots of the Tours dialog and the panel at every zoom-out stop at 1600×1000, and of the dialog and panel at 1000×600, 800×600 and 390×844, were inspected (`.cache/tours/shots/`); the first compact captures showed the panel's control row under the footer's detail switch, fixed by moving Prev / Play / Next into a fixed panel footer and trimming the compact max-height before the final captures. The Claude-in-Chrome extension was not connected in this session, so the suite and captures ran through headless Playwright Chrome (`.cache/tours/run.mjs`), the same path as the earlier screenshot scripts. Reports `tour-full` and `tour-subset` are in [validation-results.json](validation-results.json).

Because that first full run exercised `src/tour-diagnostics.ts` before 0e958c2 committed it, the suite was rerun at the committed state after the review fixes: `?tourtest&uxtest&hometest&nearbytest&cosmictest` (`tour-final`) on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max** at **1600×1000**, bracketed by `git log -1` on 33f60bd at both ends, with no page errors, passes before and after WebGL context loss and recovery. The five zoom-out framings land within **5.4 × 10⁻²⁰ Mpc** of their targets (exactly zero at four stops) and within **9.0 × 10⁻¹⁶** relative of their distances; the shell is off at stops 1–4 and on at the CMB stop; with the shell saved off, Escape after Finished hides the panel and leaves the shell, its checkbox and `atlas-cosmic-horizon` at the saved state, and with it saved on the CMB stop shows it and both Prev and Exit keep it on and saved; a wheel tick during the hop to the Milky Way pauses with "Paused · drag or scroll moved the view", Play unpressed, **0 Mpc** target drift and 5.0 × 10⁻¹⁵ relative distance drift over 300 ms; Escape exits; the road trip finishes with all **10** stops arrived and nothing skipped; the DESI pool peaks at **12** with 236 removals (185 after recovery) and **0** on-screen removals above 0.05 blend. Every shared probe passes (CMB shell, observer pass, UI, search availability, nearby search, nearby, local positions, local interaction, home navigation, home and home appearance, each also after recovery where the suite repeats it); the home orbit records **56.5 FPS, p95 16.8 ms** with 12 draws. Report `tour-final` is in [validation-results.json](validation-results.json).

## Handoff follow-up and pending tour visits — 12 September 2026

Reviewed the three commits left unconfirmed in the handoff (`33f60bd`, `0ac380b`, `5a6cfda`), including the exact committed `tour-final` report against its saved output. The review found an additional pause race: a catalog stop waiting for metadata had no animation to stop, leaving its late visit authorized. A failing deferred-visit test reproduced the Travelling state after Pause. Commit `d70630b` invalidates the pending tour token, enters Paused immediately and clears the old arrival pose; Play retries the current stop. All **85 tests in 17 files** pass, including 20 tour tests (8 data, 12 runner). Strict TypeScript, the standard build and the full Pages package pass. The separate model validator confirms **14,140,375 records**, **12,097,577 measured shapes**, **3,128 visual types**, hashes and full leaf coverage. The package remains **3,054 files, 1,078.8 MiB**, largest **3.24 MiB**.

Full-data `?tourtest&uxtest&hometest&nearbytest&cosmictest&continuitytest` (`handoff-tour-final`) and development-subset `?dataset=development&tourtest&nearbytest` (`handoff-tour-subset`) pass on HeadlessChrome 153 / **ANGLE Metal, Apple M3 Max**, **1600×1000**, bracketed at `d70630b` with no page errors. The full run holds the actual NGC 3982 metadata lookup, clicks Pause, releases the lookup and observes **0 Mpc target drift and 0 Mpc camera drift**; Play reaches the same destination. This passes both before and after WebGL context recovery. Both tours and all shared probes pass; the full road trip reaches all ten stops, and the subset skips only its two unavailable NGC stops with notices. The full tour pool peaks at **12** models, with 292 removals before recovery and 249 afterward, none on screen above 0.05 blend. Continuity records **zero abrupt events**, 110 removals all at zero blend and a 12-model peak. The short home orbit measures **56.5 FPS, p95 16.8 ms**, 12 draws and no pending, failed or budget-limited state; this is one local GPU sample, not a cross-device guarantee.

The rebuilt production package at `d70630b` passes the real controls: saved lookback/footprint settings and lookback readouts, all five zoom-out stops while autoplay is off, temporary CMB visibility and restoration on exit, R/F pausing during travel, saved-view creation/open/delete with literal names, and fresh-tab camera/selection round trips for **Andromeda and NGC 3982**. An invalid catalog identity opens camera-only with the expected notice. All tour controls pass hit tests at **1000×600, 800×600 and 390×844**. Production screenshots were inspected; the previously documented narrow-window status crowding remains. The full catalog count is unchanged and the production run records no JavaScript or shader errors. Reports `handoff-tour-final`, `handoff-tour-subset` and `handoff-production-final` are retained in [validation-results.json](validation-results.json).


## Road trip galaxy and cloud models — 12 September 2026

Feature head `2de64f1` passes **88 tests across 18 files**, strict TypeScript, `npm run build`, and `npm run test:models`: **14,140,375 model records**, **12,097,577 measured shapes**, **3,128 visual types**, all hashes and full leaf coverage. `npm run build:pages` validates the unchanged full catalog and produces **3,057 files, 1,078.8 MiB**, largest **3.24 MiB**. The public-source/history audit passes. Tight and feathered spirals preserve every sample's radius, depth, size and palette under a common seed. Cloud unit checks pin deterministic distinct fields, empty boundaries, bounded samples, source/geometry preservation, visibility and identity stability across appearance changes.

Full-data `?tourtest&cloudtest&varianttest&colortest&continuitytest&uxtest&nearbytest&hometest` (`roadtrip-models-full`) passes on **HeadlessChrome 153 / ANGLE Metal Apple M3 Max**, **1600×1000**, with no page or shader errors. All five spiral recipes pass at 0°, 60° and 85°; total light stays within **0.42%** of the classic reference, and the two new recipes have no brighter peak than it (the feathered 85° case adds four near-white pixels, within the existing eight-pixel limit). Both cloud volumes pass face/inclined/reverse/edge/inside/behind-camera views, picking, internal extinction, identical redraw and reconstruction with a changed dense ID and appearance. External peaks range from **109–174/255**, with **zero clipped pixels**, including inside views; behind-camera views produce zero draws and pixels. Cloud checksum comparisons pass across WebGL recovery. Each model uses **two draws and 671,744 tracked CPU/GPU bytes**; all six nearby models total **4,031,488 bytes**, slightly below the prior spiral-only allocation.

The full road trip reaches all ten stops before and after recovery; the guided zoom-out, saved shell restoration, manual input and pending metadata pause/resume all pass. The pool peaks at **12**; continuity records **zero abrupt events** and 110 removals, all at zero blend. Shared UI, search, nearby shapes/picking/measurement, uncertain-position safeguards and Milky Way navigation/appearance pass. Four-second cloud orbit samples after three-second warmups record **60.0 FPS / p95 16.7 ms** for LMC and **57.7 FPS / p95 16.8 ms** for SMC, with no pending, failed or blocked state. These are measurements on one GPU and a 60 Hz browser, not device-wide guarantees.

Development-subset `?dataset=development&nearbytest&cloudtest&tourtest` (`roadtrip-models-subset`) passes both before and after recovery. All six nearby models and cloud rendering checks pass; only the unavailable NGC 3982 and NGC 4026 road stops are skipped with notices. Cloud orbit samples measure 60.0 FPS on this subset. Neither catalog counts nor source measurements change.

The isolated production package passes **35 real-control assertions** (`roadtrip-models-production`): all ten road trip arrivals, cloud captions, the five named illustrations through search, cloud models retained in both appearance modes, point-display choices, a fresh-tab LMC share round trip, saved Catalog types and the original NGC 4026 lenticular model, full count and no JavaScript/shader errors. Production captures of the new stops and cloud orbits were inspected. Source stayed at `2de64f1` throughout the browser validation; the final commit records documentation/evidence only. The three final reports are retained in [validation-results.json](validation-results.json).

## Image-inspired road-trip models — 12 September 2026

Implementation head `7d11885` passes **91 tests in 19 files**, strict TypeScript, full model/hash validation and the isolated Pages package. The full archive retains **14,140,375 observations**, **12,097,577 measured shapes** and **3,128 visual types**. The package contains **3,058 files, 1,078.8 MiB**, largest **3.24 MiB**. The source/history audit passes. Primary reference images and per-stop decisions are recorded in [image-portraits.md](image-portraits.md); no telescope image assets are redistributed.

Full-data `?tourtest&portraittest&cloudtest&varianttest&colortest&continuitytest&uxtest&nearbytest&hometest` (`image-portraits-full`) passes on **Chrome 153 / ANGLE Metal Apple M3 Max, 1600×1000**, with no page or shader errors. Both routes, pause/skip behavior, settings, search, body picking, local-position guard and nearby measurements pass. All isolated model checks pass again after WebGL recovery; named disk and cloud pixel checksums are unchanged across recovery and reconstruction with different dense IDs/appearance settings.

The three disk portraits render in **one draw each**, with **1,376,248 tracked CPU/GPU texture bytes** per disk, including mipmaps. The clouds retain **two draws and 671,744 bytes each**. All six nearby models total **4,095,984 bytes**, below the existing 4 MiB default-appearance ceiling (the previous Catalog types allocation was smaller). Front/inclined/reverse/edge views preserve highlights: disk peaks **188–237/255**, cloud peaks **113–165/255**, zero clipped pixels; inside views remain finite and unclipped, and behind-camera views produce zero pixels/draws. All dust tests darken the integrated light, repeat error is zero, and small-angle redraw differences remain below 0.023 mean RGB levels. The untouched home appearance produces **exactly the same complete GPU metric report** as the previously validated cloud release despite extraction of the common ray template.

Four-second full-app orbit samples after three-second settling measure **57.7 FPS for NGC 3982, 56.2 for Andromeda, 58.2 for Triangulum, 60.0 for LMC and 57.0 for SMC**, with **p95 16.7–16.8 ms** on this 60 Hz browser session. No chunks are pending/failed and no memory limit is reached during those samples. These are local measurements, not cross-device throughput claims. The continuity replay stays within **12 resident DESI models**, with **zero abrupt events** and **118 evictions all at zero blend**. Generic spiral variation/color checks remain unchanged and pass.

Development-subset `?dataset=development&nearbytest&cloudtest&portraittest&tourtest` passes with both nearby disks/clouds and expected notices for the two unavailable DESI stops. The production package passes **54 real-control checks**, including all **ten road-trip stops**, all eight named treatments/reference links/disclosures, cloud mode changes, share restoration and persisted Catalog types. Visually inspected every stop and separate M32/M110 close-ups; the smooth dwarfs retain their small adopted scale. The Milky Way, Coma and survey keep their existing reference/catalog appearance. Operational deployment receipts remain in ignored local files.

Final dwarf-light refinement at `debd77a`: the compact M32 and diffuse M110 profiles keep their coefficients and adopted geometry, with illustrative exposures of 8 and 6 so their smooth bodies remain legible. `image-portraits-dwarfs-full` adds all three smooth portraits to the multi-angle GPU suite and passes alongside nearby, continuity, UI and home checks; `image-portraits-dwarfs-subset` passes the four available disk/smooth portraits and nearby checks. Smooth-model face-view peaks are 179–230/255; all external/inside views remain unclipped, forward-only, pickable and deterministic across reconstruction/context recovery. The rebuilt production package passes all 54 real-control checks and all ten road-trip stops again. Separate M32/M110 screenshots confirm visible smooth bodies with their different adopted sky ellipses. No other model, source measurement or size changed.

## Mobile interface browser checks

`scripts/check-mobile-ui.mjs` exercises the public controls in Chrome and WebKit: compact/scrolling inspectors, orbit/pinch/pan camera changes through saved views, settings, home/CMB/flight/tours, search cancellation, reduced viewport, portrait/landscape widths and repeated desktop restoration. Chrome touch gestures use browser-level touch injection; WebKit checks native button taps and responsive layout. Reduced-height search simulates available keyboard space; it does not exercise a physical iPhone keyboard or its safe-area hardware.

Use a separately installed Playwright (validated with 1.63.0) so browser tooling stays outside the application dependencies:

```sh
npm install --prefix .cache/capture-tools --no-save playwright@1.63.0
.cache/capture-tools/node_modules/.bin/playwright install webkit
PLAYWRIGHT_MODULE="$PWD/.cache/capture-tools/node_modules/playwright/index.mjs" node scripts/check-mobile-ui.mjs
```

The script uses installed Chrome and downloaded WebKit, a running full-data dev server at `http://127.0.0.1:5173/`, and ignored `.cache/mobile-ui/checks` output. `ATLAS_TEST_URL`, `ATLAS_TEST_OUTPUT` and `ATLAS_TEST_ENGINE=chrome|webkit` can select a production preview, output folder or engine. Browser assertions are supplemental to the existing shared GPU suites. Final browser/production results are recorded below.

## Mobile interface results — 12 September 2026

The responsive control implementation at `2ddd8d2` passes **91 tracked tests**, strict TypeScript and full-data `?uxtest&hometest&sharetest&tourtest&nearbytest` on **Chrome 153 / ANGLE Metal Apple M3 Max, 1600×1000** (`mobile-ui-full`). Inspector actions, cancelled/reopened visits, unavailable-name handling, both tour routes, shared-view restoration, home focus/wheel navigation, nearby search/picking/measurements and the local-position guard pass. Context recovery checks pass without page or shader errors. Final source `17a83f8` adds only phone-scoped identity and fallback-link typography.

The rebuilt production package passes **122 public-control browser assertions** on **Chrome 153 / ANGLE Metal Apple M3 Max** and **WebKit 26.6 / Apple GPU** (`mobile-ui-production`). Both engines retain the full **14,140,375** count. Phone widths/heights: **375×812, 402×874, 430×932, 874×402**; search also passes at **402×480**, and both engines repeatedly restore the original desktop controls at **1600×1000** without duplicate IDs or inert desktop details. The desktop header, rail, footer, inspector and action bounds match the earlier production capture; the catalog text block has a content-dependent width.

The phone checks cover compact details, bounded scroll areas, reachable Focus/Close actions, fresh detail scroll positions, source identities, settings retention, search cancellation/reopening, home/CMB/flight/measurement/tour actions and a denied-clipboard fallback with readable text. Real Chrome touch injection verifies orbit, pinch and pan using before/after cameras from the public Save view flow; selection survives those gestures. WebKit exercises native taps and responsive layout. Production screenshots of compact and expanded galaxy/tour panels, settings/menu/search, home/CMB and landscape were reviewed. The tests simulate phone viewports and reduced keyboard space; **no physical iPhone, software keyboard or hardware safe-area validation is claimed**, and no new device performance benchmark was taken.

The isolated package retains **3,058 files, 1,078.8 MiB**, largest **3.24 MiB**, with full catalog hashes verified. Application dependencies, galaxy models, positions, shapes, rendering budgets and data coverage are unchanged. Reports are appended to [validation-results.json](validation-results.json); operational deployment records remain ignored.

## Cosmic road-trip invitations

`node scripts/check-tour-invitation.mjs` uses the same `PLAYWRIGHT_MODULE`, `ATLAS_TEST_URL`, `ATLAS_TEST_OUTPUT` and optional `ATLAS_TEST_ENGINE` setup as the phone checks. It captures the public copy action, opens the invitation in a fresh browser page, verifies the first stop and natural autoplay to the LMC, pauses/exits, checks that reload does not restart, and exercises a new invitation in an already-open tab. It also covers ordinary view restoration, an unknown route, clipboard denial and navigation that cancels a deliberately delayed name-index load. Clipboard capture is test instrumentation; the destination tour runs through the real application and catalog.

Final combined implementation at `037a4ed` passes **92 tracked tests**, strict TypeScript, the isolated full-data package and the public-source audit. Full-data `?uxtest&hometest&sharetest&tourtest&nearbytest` (`mobile-tour-share-full`) passes again with the invitation startup/cancellation code, including the context recovery probes and no page/shader errors. The rebuilt production package passes all **122 phone assertions** again (`mobile-tour-share-production`) and **30 invitation assertions** (`tour-invitation-production`) across Chrome 153 and WebKit 26.6. Invitations advance naturally from the first stop to the LMC, work in a fresh or already-open tab, preserve ordinary view sharing, provide a manual clipboard fallback and yield to navigation during a delayed name request. The package retains 3,058 files / 1,078.8 MiB and full data coverage. Browser environments and the physical-device limitation remain as described above.

Development-subset `?dataset=development&nearbytest&tourtest` (`mobile-tour-share-subset`) passes, including context recovery, all six nearby entries and the expected skip notices for the two unavailable DESI road stops. The UI availability probe uses full-catalog fixtures, so the shared `uxtest` run above uses full data. All four combined-release reports are appended to the validation record.

## Default background opacity — 13 September 2026

Implementation `148495d` changes the initial point-opacity floor to **0.005 (0.5%)**, shared by the renderer and Settings. Saved percentages still win, including an explicit zero. Missing, empty, invalid or unavailable storage keeps the default. **92 tests**, strict TypeScript and the full Pages package pass; catalog coverage and assets remain unchanged.

Full-data `?selftest&uxtest&hometest` (`opacity-default-full`) passes on Chrome 153 / ANGLE Metal Apple M3 Max, including point sizing/picking, stable measurement, context recovery, home/search/UI checks and the uncertain-local guard. The isolated faint-floor shader probe now uses the actual default: it produces a maximum alpha of 1/255 over 32 pixels, with 52 nonzero GPU ID pixels; the explicit zero-floor sample remains invisible and unpickable.

The production package passes eight fresh-browser cases (`opacity-default-production`): Chrome with missing, saved-zero, saved-7.5%, invalid, empty and blocked storage; WebKit with a fresh phone viewport and saved zero. Each checks the slider, label and actual `uMinOpacity` uniform upload (0.005 for the new default). Both reports are appended to the validation record; no browser errors occurred.

## Coma tour framing — 13 September 2026

The old stop was reproduced twice on Chrome with all visible detail loaded (537,633 submitted observations); switching to full detail left that count unchanged. The new camera retains the NED center and looks outward along its sky direction from 15 Mpc in front. Projecting the 2,110 existing sky/redshift-window positions into the 1600×1000 camera puts 1,377 in the central rectangle (|NDC x| < 0.25, |NDC y| < 0.4), compared with 410 at the old pose. These are geometric projections of the window rows, not counts of rendered points or confirmed members. The resulting central gathering was visually reviewed after traversing every preceding stop.

The tour runner’s outward-framing regression failed before the change and passes afterward. All 92 tracked tests and strict TypeScript pass. Full-data `?uxtest&hometest&sharetest&tourtest` (`coma-framing-full`) passes on Chrome 153 / ANGLE Metal Apple M3 Max at 1600×1000, including the new Coma arrival check and context recovery, without page or shader errors. The isolated full-data package passes: 3,058 files, 1,078.8 MiB, largest 3.24 MiB. The production package passes normal-speed journeys through all nine stops on Chrome at 1600×1000 and WebKit at 402×874 (`coma-framing-production`): exact outward framing, unchanged catalog count, explanatory caption, and an unobstructed screen center. Both screenshots were visually reviewed; no page errors occurred. These are simulated phone dimensions, not a physical iPhone test.

The development-subset `?dataset=development&nearbytest&tourtest` run (`coma-framing-subset`) also passes, including context recovery, the new outward Coma arrival assertion and the expected skips for the two unavailable DESI close-ups. Full GPU, subset GPU and production-browser reports are appended to the validation record.
